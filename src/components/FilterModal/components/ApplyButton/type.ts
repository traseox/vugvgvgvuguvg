/**
 * Пропсы для компонента ApplyButton.
 *
 * @interface ApplyButtonProps
 * @property {function} onClick - Функция, вызываемая при нажатии на кнопку.
 */
export interface ApplyButtonProps {
  onClick: () => void
}
