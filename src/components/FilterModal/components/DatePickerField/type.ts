/**
 * Пропсы для компонента DatePickerField.
 *
 * @interface DatePickerFieldProps
 * @property {string} label - Метка для поля выбора даты.
 * @property {string} name - Имя поля, используемое для управления формой.
 */
export interface DatePickerFieldProps {
  label: string
  name: string
}
