/**
 * Пропсы для компонента StatusSelect.
 *
 * @interface StatusSelectProps
 * @property {string[]} filteredStatuses - Массив статусов, которые будут отображаться в выпадающем списке.
 */
export interface StatusSelectProps {
  filteredStatuses: string[]
}
